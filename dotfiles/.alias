alias l='ls -al'
alias v='nvim'
alias h='helix'
alias sudo='sudo env "PATH=$PATH" '
alias getip="curl https://ipinfo.io/json"
alias getweather="curl https://wttr.in"
alias getspeed="curl -s https://raw.githubusercontent.com/sivel/speedtest-cli/master/speedtest.py | python -"
alias installomz='sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"'
alias installbrew='/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"'
alias installchsrc='curl https://chsrc.run/posix | sudo bash'
alias installsingbox='curl -fsSL https://sing-box.app/install.sh | sh'
alias installxcmd='eval "$(curl https://get.x-cmd.com)"'
alias installreinstall='curl -O https://raw.githubusercontent.com/bin456789/reinstall/main/reinstall.sh || wget -O ${_##*/} $_'
alias mkdatedir='mkdir $(date "+%Y%m%d")'
alias checkrestriction="bash <(curl -L -s https://raw.githubusercontent.com/lmc999/RegionRestrictionCheck/main/check.sh)"
alias ts='date -j -f "%a %b %d %T %Z %Y" "`date`" "+%s"'
alias dt='date -j -f "%a %b %d %T %Z %Y" "`date`" "+%Y-%m-%d %H:%M:%S"'
# if [[ -d "$HOME/.trash/" ]]; then
#     alias rm=trash
#     function trash(){mv -f $@ $HOME/.trashbin/}
#     function cleartrash(){/usr/bin/rm -rf $HOME/.trashbin/*}
# fi
